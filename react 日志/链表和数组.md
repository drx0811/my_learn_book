# 链表和数组

## 数组

> 数组的优势：数组方便遍历查找，但是对数组和删除的时候 操作时间复杂度为O(n)；

## 链表

> 链表的操作要相对于数组方便的多，时间复杂度O(1);
>
> 链表的存储可以是不连续的，因此可以利用不连续的内存，可以充分利用碎片化的内存

### 链表特点

- 每一个节点（node）都由<u>*`数据本身`*</u>和一个<u>*`指向后续节点的指针`*</u>组成；
- 整个链表的存取都必须从头指针开始，头指针指向第一个节点；
- 最后一个节点的指针指向空（null）

